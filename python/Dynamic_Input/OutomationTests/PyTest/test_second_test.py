def test_one_login_logout():
        print("Test 1")
#@pytest.mark.skipif(a>100),reason"This is a skipped test"
def test_two_login_logout():
        print("Test 2")

# pytest -s  folder/file name (if individual test need to be run) -> to display the print statement
# pytest -v -> to display which test are pass or fail
